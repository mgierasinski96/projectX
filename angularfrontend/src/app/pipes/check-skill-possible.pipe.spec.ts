import { CheckSkillPossiblePipe } from './check-skill-possible.pipe';

describe('CheckSkillPossiblePipe', () => {
  it('create an instance', () => {
    const pipe = new CheckSkillPossiblePipe();
    expect(pipe).toBeTruthy();
  });
});
