import { GetNextLvlExpPipe } from './get-next-lvl-exp.pipe';

describe('GetNextLvlExpPipe', () => {
  it('create an instance', () => {
    const pipe = new GetNextLvlExpPipe();
    expect(pipe).toBeTruthy();
  });
});
