import { SkillPricePipe } from './skill-price.pipe';

describe('SkillPricePipe', () => {
  it('create an instance', () => {
    const pipe = new SkillPricePipe();
    expect(pipe).toBeTruthy();
  });
});
